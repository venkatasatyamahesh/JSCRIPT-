function CreateObject(arr) {
    if (operation === "add"){
        movies.push(movie);
        return movies;
    }
    else if(operation === "remove"){
        movies.pop();
        return movies;
    }
};
//module.exports = CreateObject;
